# `switch_app/romfs`

Assets que se empaquetan dentro del `.nro`.

- `ui/icons/`: iconos finales usados por el menú y estados.
- `ui/animation/`: frames finales de la animación de transferencia.

Regla: si algo está aquí, debe ser necesario en tiempo de ejecución.

